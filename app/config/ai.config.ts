// Configuración por defecto de Theo Core (modo global)
export const defaultAIConfig = {
  provider: "google", // alias gemini
  model: "gemini-2.5-flash",
  apiKey: process.env.GOOGLE_API_KEY,
  mode: "asistente",
};
